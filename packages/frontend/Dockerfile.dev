FROM node:18-alpine

WORKDIR /app

# Copia file di dipendenze
COPY package*.json ./

# Installa dipendenze
RUN npm install

# Espone la porta
EXPOSE 3000

# Avvia il server di sviluppo
CMD ["npm", "run", "dev", "--", "--host", "--port", "3000"]
